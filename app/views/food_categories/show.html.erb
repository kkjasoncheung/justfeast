<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @food_category.name %>
</p>

<p>
  <strong># of restaurants with this:</strong>
  <%= @food_category.restaurants.size %>
</p>

<p>
  <strong>Restaurants serving this:</strong>
  <ul>
	  <% @food_category.restaurants.each do |rest| %>
	  	<li><%= link_to(rest.name, restaurant_path(rest.id)) %></li>
	  <% end %>
 </ul>
</p>


<%= link_to 'Edit', edit_food_category_path(@food_category) %> |
<%= link_to 'Back', food_categories_path %>
